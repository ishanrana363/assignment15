import React from 'react';
import SaveData from "../components/SaveData.jsx";

const SaveDataPage = () => {
    return (
        <div>
            <SaveData/>
        </div>
    );
};

export default SaveDataPage;